APP_NAME=San_Miguel
APP_ENV=testing
APP_KEY=base64:TeJakJoTs2QG4h8lGFSVNVJI8ld6G2pt+sXxvty0JSM=
APP_DEBUG=false
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=4

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database para tests (SQLite en memoria o PostgreSQL en CI)
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=sanmiguel_test
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Sesiones en array para tests (más rápido)
SESSION_DRIVER=array
SESSION_LIFETIME=120

# Sanctum
SANCTUM_STATEFUL_DOMAINS=localhost

# Broadcasting & Queue en sync para tests
BROADCAST_CONNECTION=log
QUEUE_CONNECTION=sync

# Cache en array para tests
CACHE_STORE=array
CACHE_PREFIX=sanmiguel_test

# Redis para tests (opcional, puede usar array)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CACHE_DB=3
REDIS_SESSION_DB=4
REDIS_QUEUE_DB=5

# Mail en log para tests
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="test@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Filesystem local para tests
FILESYSTEM_DISK=local

# AWS (no usar en tests)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false
